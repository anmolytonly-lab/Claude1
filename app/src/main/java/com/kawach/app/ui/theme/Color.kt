package com.kawach.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette — deep teal/blue ──────────────────────────────────────
val TealPrimary = Color(0xFF00607A)
val TealVariant = Color(0xFF004E64)
val TealLight = Color(0xFF4DD0E1)
val TealContainer = Color(0xFFB2EBF2)

// ── SOS / Emergency — reserved red ────────────────────────────────────────
// Red is intentionally ONLY used for SOS-related actions.
val SosRed = Color(0xFFD32F2F)
val SosRedDark = Color(0xFF9A0007)
val SosRedLight = Color(0xFFFF6659)
val SosRedContainer = Color(0xFFFFCDD2)

// ── Neutral / Surface ─────────────────────────────────────────────────────
val SurfaceLight = Color(0xFFF5F5F5)
val SurfaceDark = Color(0xFF121212)
val OnSurfaceLight = Color(0xFF1C1C1C)
val OnSurfaceDark = Color(0xFFE0E0E0)

// ── Safe / Success ────────────────────────────────────────────────────────
val SafeGreen = Color(0xFF388E3C)
val SafeGreenContainer = Color(0xFFC8E6C9)
